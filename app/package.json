{
  "name": "nixie-clock-mqtt",
  "description": "MQTT bridge for integrating Clocteck RGB Tube Clock with Home Assistant",
  "version": "1.3.2",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "mqtt": "^5.3.0",
    "zod": "^3.23.0"
  }
}
